import 'package:flutter/material.dart';

class HistoryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Halaman Riwayat akan menampilkan daftar belanja yang telah diselesaikan.',
      ),
    );
  }
}
